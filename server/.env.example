# 数据库连接（与 docker-compose.yml 一致）
DATABASE_URL="mysql://fyp:fyp_pass@localhost:3306/fyp_selection"

# JWT 密钥与有效期
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="7d"

# 服务端口
PORT=4000

# 前端地址（CORS 白名单）
CLIENT_ORIGIN="http://localhost:5173"
